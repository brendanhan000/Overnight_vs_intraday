# Core
pandas>=2.0
numpy>=1.24
pyarrow>=12.0
requests>=2.28
PyYAML>=6.0

# Data sources
yfinance>=0.2        # backtest data source (raw OHLC + split/div-adjusted close)
lxml>=4.9            # parse the S&P 500 constituents table from Wikipedia
schwab-py>=1.3       # Schwab Trader (Market Data) API — only for --source schwab

# Stats / factors (Layer 1+)
statsmodels>=0.14
scipy>=1.10
pandas-datareader>=0.10   # Fama-French factors via Ken French library

# Plots
matplotlib>=3.7

# Dev
pytest>=8.0
